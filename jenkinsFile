pipeline  {

    agent any
    
    tools {nodejs "nodejs"} 
    
    stages {
        
        stage('Checkout'){
            steps {
                git branch: 'main', 
                credentialsId: 'git-cred-todoapp', 
                url: 'https://github.com/adewalegbadamosi/todoapp'
            }
        }

        stage('Build') {
          
            steps {
            
                sh """                
                    node --version && \
                    npm install && \
                    npm run build  
                """
                
            }
        }
   
    }
}
